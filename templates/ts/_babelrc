{
  "presets": [
    ["@babel/preset-env", {
      "modules": false,
      "targets": {
        "browsers": "ie >= 11"
      }
		}],
		"@babel/preset-typescript"
  ],
  "env": {
    "test": {
      "presets": [["@babel/preset-env"]]
    }
  }
}
